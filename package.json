{
  "name": "veilfall",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.js",
    "start": "npm run build && node scripts/serve.js --dist",
    "build": "node scripts/build.js",
    "test": "node --test"
  }
}
